<?xml version="1.0"?>
<odoo>
    <record id="estate_property_view_search" model="ir.ui.view">
        <field name="name">estate.property.view.search</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <search string="Search Opportunities">
                <filter name="available" string="Available" domain="[('state', 'in', ['offer received', 'new'])]"/>    
                <field name="name"/>
                <field name="property_type_id"/>
                <field name="postcode"/>
                <field name="expected_price"/>
                <field name="bedrooms"/>
                <field name="living_area" filter_domain="[('living_area', '>=', self)]"/>
                <field name="facades"/>
                <separator/>
                <filter string="Available Properties" name="Available Properties" domain="['|', ('state', '=', 'new'), ('state', '=', 'offer received')]"/>
                <separator/>
                <group expand="1" string="Group By">
                    <filter string="Postcode" name="Postcode" context="{'group_by':'postcode', 'residual_visible':True}"/>
                </group>
            </search>
        </field>
    </record>

    <record id="estate_property_view_form" model="ir.ui.view">
        <field name="name">estate.property.form</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <form string="Property">
                <header>
                    <button name="sell_property" type="object" string="SOLD" invisible = "state != 'offer accepted' "/>
                    <button name="cancel_property" type="object" string="CANCEL" invisible = "state == 'sold' "/>
                    <field name="state" widget="statusbar" statusbar_visible="new,offer received,offer accepted,sold"/>
                </header>
                <sheet>
                    <group>
                        <div>
                            <h1><field name="name"/></h1>
                            <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}"/>
                        </div>
                        <div></div>

                        <div class="row">
                        <group class="col">
                            <field name="state"/>
                            <field name="property_type_id" options="{'no_create': true, 'no_create_edit': true}"/>
                            <field name="postcode"/>
                            <field name="date_availability" string="Available From"/>
                        </group>
                        <group class="col">
                            <field name="expected_price"/>
                            <field name="best_price"/>
                            <field name="selling_price"/>
                        </group>
                        </div>
                    </group>
                    <notebook>
                        <page string="Description">
                        <group>
                            <field name="description"/>
                            <field name="property_type_id" options="{'no_create': true, 'no_create_edit': true}"/>
                            <field name="bedrooms"/>
                            <field name="living_area"/>
                            <field name="facades"/>
                            <field name="garage"/>
                            <field name="garden"/>
                            <field name="garden_area" invisible="garden == 0"/>
                            <field name="garden_orientation" invisible="garden == 0"/>
                            <field name="total_area"/>
                            <field name="state"/>
                            <field name="active"/>
                        </group>
                        </page>

                        <page string="Offers">
                            <field name="offer_ids" readonly="state == 'offer accepted' or state == 'sold' or state == 'canceled'"/>
                        </page>

                        <page string="Other Info">
                        <group>
                            <field name="salesperson"/>
                            <field name="buyer"/>
                        </group>
                        </page>

                    </notebook>
                </sheet>
            </form>
        </field>
    </record>

    <record id="estate_property_view_list" model="ir.ui.view">
        <field name="name">estate.property.list</field>
        <field name="model">estate.property</field>
        <field name="arch" type="xml">
            <list string="Channel" decoration-success="state=='offer received'  or state=='offer accepted'" decoration-bf="state=='offer accepted'" decoration-muted="state=='sold'">
                <field name="name" string="Title"/>
                <field name="tag_ids" widget="many2many_tags" options="{'color_field': 'color'}" />
                <field name="property_type_id" options="{'no_create': true, 'no_create_edit': true}"/>
                <field name="postcode"/>
                <field name="bedrooms"/>
                <field name="living_area"/>
                <field name="expected_price"/>
                <field name="selling_price"/>
                <field name="date_availability" string="Available From" optional="hide"/>
            </list>
        </field>
    </record>

    <record id="estate_property_action" model="ir.actions.act_window">
        <field name="name">Properties</field>
        <field name="res_model">estate.property</field>
        <field name="view_mode">list,form</field>
        <field name="context">{'search_default_available': True}</field>
    </record>

    <record id="estate_property_type_action" model="ir.actions.act_window">
        <field name="name">Property Type</field>
        <field name="res_model">estate.property.type</field>
        <field name="view_mode">list,form</field>
    </record>

    <record id="estate_property_tag_action" model="ir.actions.act_window">
        <field name="name">Property Tags</field>
        <field name="res_model">estate.property.tag</field>
        <field name="view_mode">list,form</field>
    </record>
    

</odoo>